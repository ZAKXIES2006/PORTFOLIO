 Topic 2 Reflection

Procedural vs class-based: Grouping hours, rate, and taxRate into a Payroll class bundled data with behaviour and centralised validation in property setters ...no more repeated checks or invalid state.

Static typing: Errors caught at compile time instead of runtime. More verbose than Python, but safer refactoring and better IntelliSense make the tradeoff worth it.